import { Box, Heading } from '@chakra-ui/react';
import Nav from '../components/Nav';

const GuidesPage = () => {
  return (
    <Box>
      <Nav />
      <Box>
        <Heading>This is where all the guides live</Heading>
      </Box>
    </Box>
  );
};

export default GuidesPage;
